.backend-page-header
  %h1= t("t-page-topic-index")

.main-content-block
  %ul.main-listing
    - T.sorted.each do |trans|
      - next if trans.backend_translation?
      = render template: "t/_t", locals: {trans: trans}
