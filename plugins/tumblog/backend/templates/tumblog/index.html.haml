.backend-page-header
  %h1= t("tumblog-page-topic-index")
#db-blog-list
  %ul.main-listing.main-content-block
    - pager.each do |post|
      = render template: "tumblog/_preview", locals: {post: post}

  = pager.navigation

