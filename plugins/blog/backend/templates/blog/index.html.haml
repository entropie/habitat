.backend-page-header
  %h1= t("blog-page-topic-index")
#db-blog-list
  %ul.main-listing.main-content-block
    - pager.each do |post|
      = render template: "blog/_preview", locals: {post: post}

  = pager.navigation
